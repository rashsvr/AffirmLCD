import 'package:flutter/material.dart';

class WidgetDesign {
  static const lcdBackground = Color(0xffb8c6a2);
  static const appBackground = Color(0xff10140f);
  static const textPrimary = Color(0xff162113);
  static const textMuted = Color(0xff53624b);
  static const radius = 10.0;
  static const smallPadding = 12.0;
  static const mediumPadding = 12.0;

  static const suffixEmoji = '☀';

  static const fontFamily = 'monospace';
}
